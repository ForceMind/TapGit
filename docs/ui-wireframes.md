# 码迹（TapGit）低保真线框说明

## 1. 全局布局
```text
┌──────────────────────────────────────────────────────────────┐
│ 顶栏：项目名 | 当前方案 | 最近保存 | 同步状态                 │
├───────────────┬──────────────────────────────────────────────┤
│ 左侧导航      │ 主内容区                                     │
│ - 首页        │                                              │
│ - 当前修改    │                                              │
│ - 保存记录    │                                              │
│ - 我的方案    │                                              │
│ - 设置        │                                              │
├───────────────┴──────────────────────────────────────────────┤
│ 底部状态区：最近操作结果 / 风险提醒                           │
└──────────────────────────────────────────────────────────────┘
```

## 2. 首页线框
```text
[最近项目]
- 项目 A  最近打开时间   [打开]
- 项目 B  最近打开时间   [打开]

[当前项目状态]
- 当前方案：主线
- 未保存修改：12 个文件
- 最近一次保存：今天 14:20

[快速操作]
[查看当前修改] [保存进度] [查看保存记录] [创建新方案]
```

## 3. 当前修改页线框
```text
┌ 修改列表（左） ─────────┬ 变化详情（右） ─────────────────────┐
│ 筛选：全部/新增/修改/删除 │ 文件：src/app.tsx                   │
│ □ src/app.tsx   +20 -4   │ [分块变化视图]                      │
│ □ src/main.ts   +3  -1   │ - 旧内容                            │
│ □ README.md     +12 -0   │ + 新内容                            │
└──────────────────────────┴─────────────────────────────────────┘
[进度说明输入框：这次主要改了什么？]
[保存选中文件] [保存全部修改]
```

## 4. 保存记录页线框
```text
┌ 时间线（左） ────────────┬ 记录详情（右） ─────────────────────┐
│ 15:30 修复登录流程        │ 说明：修复登录页状态切换            │
│ 14:20 新增设置页          │ 时间：2026-04-09 14:20              │
│ 昨天 21:12 首页改版       │ 变更：8 个文件                       │
│                          │ [查看当时变化] [恢复到这里]         │
└──────────────────────────┴─────────────────────────────────────┘
```

## 5. 我的方案页线框
```text
[方案列表]
- 主线（当前） 最近保存：今天 15:30
- 登录新方案   最近保存：今天 14:00
- AI 重构尝试   最近保存：昨天 19:20

[操作]
[创建新方案] [切换到选中方案] [合并到主线]

[合并流程区域]
来源方案：登录新方案 -> 目标方案：主线
[开始合并]
若有冲突：显示“同一处被两边修改，需要你决定保留哪一份”
```

## 6. 冲突处理页（弹层）线框
```text
文件：src/login.tsx   进度：1/3
┌ 左边（当前方案版本） ───────┬ 右边（要合并进来的版本） ───────┐
│ ...                        │ ...                            │
└────────────────────────────┴────────────────────────────────┘
[保留左边] [保留右边] [手动编辑后保存]
```

## 7. 设置页线框
```text
[环境检测]
版本保护引擎（Git）：已安装 / 未安装
[重新检测] [安装引导]

[使用偏好]
- 高级模式（显示技术细节）[开关]
- 恢复前自动创建安全快照 [开关]
- 合并前自动创建安全快照 [开关]

[问题排查]
[导出日志]
```
